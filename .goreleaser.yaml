version: 2
project_name: umbrel-dropbox-sync
builds:
  - id: cli
    main: ./cmd/umbrel-dropbox-sync
    binary: umbrel-dropbox-sync
    goos: [linux]
    goarch: [amd64, arm64]
  - id: daemon
    main: ./cmd/umbrel-dropbox-syncd
    binary: umbrel-dropbox-syncd
    goos: [linux]
    goarch: [amd64, arm64]
nfpms:
  - id: deb
    package_name: umbrel-dropbox-sync
    formats: [deb]
    maintainer: Earl Co <earl@aops.studio>
    description: Reliable Dropbox sync daemon for Linux and Umbrel.
    license: MIT
    bindir: /usr/bin
    contents:
      - src: packaging/systemd/umbrel-dropbox-sync.service
        dst: /usr/lib/systemd/user/umbrel-dropbox-sync.service
      - src: desktop/dolphin/umbrel-dropbox-sync.desktop
        dst: /usr/share/kio/servicemenus/umbrel-dropbox-sync.desktop
    scripts:
      postinstall: packaging/deb/postinst
archives:
  - formats: [tar.gz]
checksum:
  name_template: checksums.txt
release:
  github:
    owner: earlvanze
    name: umbrel-dropbox-sync
